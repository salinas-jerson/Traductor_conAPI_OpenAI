npm create vite@latest
y
nombre -> elegir react - typescript + smc

en el editor: terminal
npm i
npx eslint --init

npm i react-bootstrap bootstrap -E

levantar el serv:
npm run dev
----------------------------------------------------------
js-module
typescript
coniguracion en js

fluid

----------------------
se quedo corto en el intercambio del idioma

typos de atributos: types.d.ts <- estado inicial
export type Language = keyof typeof SUPPORTED_LANGUAGES
con las llaves del objeto:

